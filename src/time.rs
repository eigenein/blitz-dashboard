use std::time::Duration as StdDuration;

pub const DAY: StdDuration = StdDuration::from_secs(86400);
pub const MINUTE: StdDuration = StdDuration::from_secs(60);
pub const FIVE_MINUTES: StdDuration = StdDuration::from_secs(300);
